# E-commerce Product URL Crawler

A scalable and asynchronous web crawler that discovers product URLs across e-commerce websites.

## Features

- Asynchronous crawling with aiohttp
- Domain-specific URL pattern filtering
- Outputs product URLs in JSON format

## Run Instructions

```bash
pip install -r requirements.txt
python crawler.py
```
